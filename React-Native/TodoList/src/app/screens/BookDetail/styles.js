import { StyleSheet } from 'react-native';
import { OLD_BOOK_COLOR } from '../../../constants/colors';

const styles = StyleSheet.create({
  detailContainer: {
    backgroundColor: OLD_BOOK_COLOR,
    borderWidth: 1,
    borderRadius: 5,
    padding: 15
  }
});

export default styles;
